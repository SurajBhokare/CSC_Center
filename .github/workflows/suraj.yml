name: Suraj workflow
on: 
  push:
jobs: 
      build: # job's name
        runs-on: ubuntu-latest # github runner(free)
        steps:
          - uses: actions/checkout@v4 # clone the code on ubuntu runner
      